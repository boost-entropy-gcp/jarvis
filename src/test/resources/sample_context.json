[
  {
    "id": "{id, same as the folder name in the assert and pre folders}",
    "contextType": "BigQuery",
    "parameters": {
      "project": "scoot-edw-dev",
      "datasetNamePrefix": "csk_",
      "edwCoreDataset": "EDW_CORE",
      "stagingDataset": "STAGING"
    }
  },
  {
    "id": "local",
    "contextType": "LocalContext",
    "parameters": {
      "repositoryRoot": "{absolute path of the project folder}"
    }
  },
  {
    "id": "{id, name of the sql file}",
    "contextType": "MySQL",
    "parameters": {
      "host": "{db host}",
      "port": "{db port}",
      "database": "{database}",
      "user": "{username}",
      "password": "{password}"
    }
  },
  {
    "id": "{id, name of folder}",
    "contextType": "MSSQL",
    "parameters": {
      "host": "{db host}",
      "port": "{db port}",
      "database": "{database}",
      "user": "{username}",
      "password": "{password}"
    }
  },
  {
    "id": "SFTP-password",
    "contextType": "SFTP",
    "parameters": {
      "host": "{db host}",
      "port": "{db port}",
      "user": "{username}",
      "password": "{password}",
      "remoteBasePath": "/out"
    }
  },
  {
    "id": "TalendAPI",
    "contextType": "TalendAPI",
    "parameters": {
      "apiUrl": "{talend API url}",
      "apiKey": "{API key}",
      "environment": "{env}",
      "workspace": "{workspace}"
    }
  }
]
